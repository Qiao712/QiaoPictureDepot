<?xml version="1.0" encoding="UTF8" ?>      <!--将encoding="UTF-8"中间的‘-’去掉->"UTF8"就中文注释就不会导致异常了-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiao.picturedepot.dao.AlbumMapper">
    <select id="getById" resultType="Album">
        SELECT id, name, owner_id, is_public, description, create_time, update_time
        FROM album
        WHERE id = #{id}
    </select>

    <select id="listByOwnerUsername" resultType="Album">
        SELECT id, name, owner_id, is_public, description, create_time, update_time
        FROM album
        <where>
            owner_id = (SELECT id FROM user WHERE username = #{ownerUsername})
            <if test="showPrivate == false">
                AND is_public = true
            </if>
        </where>
    </select>


    <insert id="add" parameterType="Album" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO album(name, owner_id, is_public, description)
        VALUES(#{name}, #{ownerId}, #{isPublic}, #{description})
    </insert>

    <update id="updateByIdAndOwnerId" parameterType="Album">
        UPDATE album
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="isPublic != null">
                is_public = #{isPublic},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
        </set>
        WHERE id = #{id} AND owner_id = #{ownerId}
    </update>

    <delete id="deleteById">
        DELETE FROM album
        WHERE id = #{id}
    </delete>
</mapper>