<?xml version="1.0" encoding="UTF8" ?>      <!--将encoding="UTF-8"中间的‘-’去掉->"UTF8"就中文注释就不会导致异常了-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiao.picturedepot.dao.AlbumMapper">
    <select id="getAlbums" resultType="Album">
        SELECT id, name, owner_id, is_public, secret_key, create_time, update_time
        FROM album
        <where>
            owner_id = (SELECT id FROM user WHERE username = #{ownerUsername})
            <if test="showPrivate == false">
                AND is_public = true
            </if>
        </where>
    </select>

    <select id="getAlbumsByUserId" resultType="Album">
        SELECT id, name, owner_id, is_public, secret_key, create_time, update_time
        FROM album
        WHERE owner_id = #{userId}
    </select>

    <select id="getAlbumsByUsername" resultType="Album">
        SELECT id, name, owner_id, is_public, secret_key, create_time, update_time
        FROM album
        WHERE owner_id = (SELECT id FROM user WHERE username = #{username})
    </select>

    <select id="getAlbumById" resultType="Album">
        SELECT id, name, owner_id, is_public, secret_key, create_time, update_time
        FROM album
        WHERE id = #{id}
    </select>

    <insert id="addAlbum" parameterType="Album" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO album(name, owner, is_public, secret_key)
        VALUES(#{name}, #{ownerId}, #{isPublic}, #{secretKey})
    </insert>

    <update id="updateAlbum" parameterType="Album">
        UPDATE album
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="ownerId != null">
                owner_id = #{ownerId},
            </if>
            <if test="isPublic != null">
                is_public = #{isPublic},
            </if>
            <if test="secretKey != null">
                secret_key = #{secretKey},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteAlbumById">
        DELETE FROM album
        WHERE id = #{id}
    </delete>
</mapper>