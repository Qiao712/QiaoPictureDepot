<?xml version="1.0" encoding="UTF8" ?>      <!--将encoding="UTF-8"中间的‘-’去掉->"UTF8"就中文注释就不会导致异常了-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qiao.picturedepot.dao.AlbumMapper">
    <select id="getAlbums" resultType="Album">
        select id, album_name, owner, is_public, secret_key, create_time, update_time from album
        <where>
            owner = (select id from user where username = #{ownerName})
            <if test="showPrivate == false">
                and is_public = true
            </if>
        </where>
    </select>

    <select id="getAlbumsByUserId" resultType="Album">
        select id, album_name, owner, is_public, secret_key, create_time, update_time from album
        where owner = #{userId}
    </select>

    <select id="getAlbumsByUsername" resultType="Album">
        select id, album_name, owner, is_public, secret_key, create_time, update_time from album
        where owner = (select id from user where username = #{username})
    </select>

    <select id="getAlbumById" resultType="Album">
        select id, album_name, owner, is_public, secret_key, create_time, update_time from album
        where id = #{id}
    </select>

    <insert id="addAlbum" parameterType="Album" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into album(album_name, owner, is_public, secret_key)
        values(#{albumName}, #{owner}, #{isPublic}, #{secretKey})
    </insert>

    <update id="updateAlbum" parameterType="Album">
        update album
        <set>
            <if test="albumName != null">
                album_name = #{albumName},
            </if>
            <if test="owner != null">
                owner = #{owner},
            </if>
            <if test="isPublic != null">
                is_public = #{isPublic},
            </if>
            <if test="secretKey != null">
                secret_key = #{secretKey},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteAlbumById">
        delete from album
        where id = #{albumId}
    </delete>
</mapper>